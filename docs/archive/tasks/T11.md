# T11. Documentation refresh

**Depends on** T8 (done) · parallel-safe with T9
**Read first:** `AGENTS.md`, `README.md` (what you are replacing), `ROADMAP.md`, `DESIGN.md`

`README.md` still describes the project as it was before any of this existed. It documents the old warm palette, says nothing about the creature, the API, or the extension, and omits a frontmatter field that already ships.

## Files you own

```
README.md
.env.example        (only if something is missing from it)
```

## Files you must NOT touch

- `DESIGN.md`, `ROADMAP.md`, `tasks/**`. Those are working documents, not user docs.
- Everything under `src/` and `extension/`. T9 is live in `src/lib/game/` and `src/app/api/creature/route.ts` right now.

## What is wrong today

1. **Tech stack section** describes the pre-creature site.
2. **The creature system is entirely undocumented.** No mention of XP, stages, items, or how any of it is computed.
3. **`maturity` frontmatter is undocumented.** It ships and works (`seedling`, `budding`, `evergreen`, defaulting to `seedling` when absent) but appears nowhere in the frontmatter reference table.
4. **No mention of `GITHUB_TOKEN` or `GITHUB_LOGIN`**, both of which the build reads. Without a token the commit path silently undercounts by roughly 10x, and nothing tells a reader that.
5. **The API and the extension do not exist** in the docs at all.
6. Colour references, if any, point at the retired palette.

## What to write

Keep the existing voice. It is direct and unfussy, do not make it corporate.

- **What this is.** A digital garden where writing notes and shipping code grow a pixel creature.
- **The creature system.** The four stages and their thresholds, what earns XP and at what rate, and how items unlock. Pull real numbers from `src/lib/game/types.ts`, do not invent them. Explain the design intent: XP rewards *connection*, since wikilinks and backlinks score well while raw word count deliberately does not.
- **Setup.** `npm install`, `.env.local` with `GITHUB_TOKEN` and `GITHUB_LOGIN`, `npm run dev`. State plainly that without a token the unauthenticated path undercounts commits by roughly 10x, because GitHub's public events API no longer returns per-push commit counts.
- **Writing content.** Keep and correct the existing sections. Add `maturity` to the frontmatter table as optional. Confirm the wikilink docs mention the `[[Target|alias]]` form, which works now.
- **The API.** Both endpoints with example requests and what comes back. Note the caching behaviour and that it works for any public GitHub handle.
- **The README badge.** A copy-pasteable markdown snippet. Note that it is static because GitHub's camo proxy strips SVG animation.
- **The extension.** How to load it unpacked, what it does, and that it is not on the Chrome Web Store.
- **Development.** `npm test`, `npm run build`, and where the task specs live for anyone picking up the remaining work.

## Rules

- **Verify every claim against the code.** Do not copy numbers from `ROADMAP.md` or `DESIGN.md`, which may drift. `types.ts` is the source of truth for stages, rates, and caps.
- Zero em-dash characters.
- Every command you write must actually run. Try them.
- No invented features. If something is half-built, either omit it or label it clearly.
- Do not document the Pokemon sprite situation as a legal position. One neutral sentence noting sprites come from PokeAPI at runtime and are not bundled is enough.

## Done when

- Every command in the README has been executed and works.
- Every number traces to `types.ts`.
- A reader who has never seen this repo could clone it, set it up, write a note, and understand why their creature levelled up.

## Report

What you changed, any claim in the old README that turned out to be wrong, and anything you found undocumented that you chose not to add.
